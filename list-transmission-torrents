#!/bin/bash
DIR="$HOME/.config/transmission-daemon/torrents"
FILES=$(ls -t $DIR)
IFS=$'\n'
for FILE in $FILES; do
  AGE=$(($(date +%s) - $(stat -c %Y $DIR/$FILE)))
  MAGE=$(($AGE / 86400))
  HAGE=$(($MAGE / 60))
  DAGE=$(($HAGE / 24))
  echo $(printf "%02dd" "$MAGE") $FILE
done
